fun main(){
    var city = "New York"
    var temperature = 22.5

    println("the type if city is ${city.javaClass.kotlin}")
    println("the type if temperature is ${temperature.javaClass.kotlin}")
}